{% extends 'layout.html.twig' %}

    {% block container %}
        <!-- Post Section-->
        <div class="post-container">
            <div class="post-list">
                {{ include("homepage/post.html.twig") }}
            </div>
        </div>
    {% endblock %}
